export type ListVariant = 'default';

export interface ListVariantConfig {
  // List-specific styling can be added here if needed
}

export const LIST_VARIANTS: Record<ListVariant, ListVariantConfig> = {
  default: {
    // Default list styling
  },
};
